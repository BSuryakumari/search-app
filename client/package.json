{
  "name": "simple-interest-calculator",
  "version": "1.0.0",
  "description": "Simple interest calculator web application using the MERN stack",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run start\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "concurrently": "^6.2.1",
    "express": "^4.17.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=12.0.0"
  }
}
